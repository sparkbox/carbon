// webpack will load styles in development
// in production, this will be extracted and loaded as a proper CSS file
// import './styles.scss';

// all polyfills will be conditionally loaded via dynamic imports
// this requires promise to first be in place
// import 'core-js/features/promise';

// const polyfillsLoaded = import('./polyfills' /* webpackChunkName: 'polyfills' */)
// .then(exports => exports.loaded);

// const main = import('./main' /* webpackChunkName: 'main' */);

// polyfillsLoaded.then(() => main.then(exports => exports.default()));
